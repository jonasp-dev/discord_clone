meta {
  name: Leave Server
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/servers/{{serverId}}/leave
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
