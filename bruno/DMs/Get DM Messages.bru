meta {
  name: Get DM Messages
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/dms/{{conversationId}}/messages
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  ~cursor: 
  ~limit: 50
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response has paginated messages", function() {
    expect(res.body.data).to.be.an('array');
    expect(res.body).to.have.property('hasMore');
  });
}
